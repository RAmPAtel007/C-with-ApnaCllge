// cube of the number 
    #include<stdio.h>

int main(){
    int num;
    printf("Enter the num for cube :");
    scanf("%d", &num);

    printf("the cube of the num is %d", num*num*num );
    return 0;
}
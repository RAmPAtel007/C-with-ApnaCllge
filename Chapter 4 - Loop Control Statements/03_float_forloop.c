    #include<stdio.h>

int main(){
     for(float i= 1.0; i<=5.0 ; i++ ) { 
        printf("%f\n", i);
        }

        for(char ch='a'; ch <='z'; ch++){
            printf("%c\n", ch);
        }


        // infinite loop don't run 

        
        // syntax ; for (i =1;   ; i++);
        // printf("this is infinite loop it will run till you memory of computer is not going to fill full");
    return 0;
}